[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = ">=4.2"
djangorestframework = ">=3.14"
gunicorn = ">=20.1"
django-cors-headers = ">=3.15"
whitenoise = ">=6.0"

[dev-packages]

[requires]
python_version = "3.12"
